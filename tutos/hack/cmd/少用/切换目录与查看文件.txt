cd.. 返回上一级  （change direction）

cd.  现实当前路径   

cd/   切到X的根目录
如C：\users   会回到C：\
其次，输入X：    C：
可切换到X盘、C盘

dir /a    显示所有文件，包括隐藏
可以添加属性，
（比如文档文件属性是“可存档”）
可用于筛选
例：dir /ah  （h的意思的hide即
查看隐藏文件）

dir /ar 找到只读文件

dir /as（系统文件）

dir /on  按名字顺序排列  S为大小
E为扩展名  D为日期 
  