rem 解释说明，不会允许
相当于：：

exit退出

加@不显示命令
echo off 不显示用户

:XX（在goto前goto后无所谓）
goto XX

start 可开网址或程序，但是在外面打开
start /min（/max） 程序或目录
最小化/大化打开
/B 启动应用但不打开窗口
在某盘内，要 start "" "XXX"

call 可开程序，连接另外一个程序，在里面打开（需要在同一目录，否则要加路径）相当于续接命令

