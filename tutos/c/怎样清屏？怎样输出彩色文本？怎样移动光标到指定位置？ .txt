#include<conio.h>
需要使用 termcap, term-info 或 curses 类的函数库, 或者系统提供的特殊函数。 在 MS-DOS 系统下, 有两个函数可以使用 clrscr() 和 gotoxy()。

有一个不彻底的可移植的清屏方法: 输出卷纸字符  ('\f'), 可以清除一部分 的显示。还有个更加可移植的办法 (尽管很简陋), 输出足够多的换行 使当前屏幕清空。 最后一个方法: 使用 system() 函数 来调用操作系统 的清屏指令。 