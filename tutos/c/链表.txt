先malloc一个指针，此时就是新的结构
一般有个无数据的头指针head
建立时如果是第一次（head->next==NULL），让head指向这个新的

在交换时要记录前一个的位置然后让前一个指向这个的后一个

删除用free(指针)

排序要记住记录前一个最大或最小项，然后让最大或最小项排到这一项后面
这时就是有头指针的优越处了，永远不会是第一个指针，所以可以放心修改，因为如果头指针也有数据，但它没有再前一项，会比较难改

如果要操作多个链表（比如，不同的班级链表），这时候就需要一个头指针的指针来切换
删除时要记得如果删除头指针则会改变链表，所以要返回指针

