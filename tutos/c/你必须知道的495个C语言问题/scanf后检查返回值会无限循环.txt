scanf() 转换数字的时候, 它遇到的任何非数字字符都会终止转换 并被保留在输入流中。

因此, 除非采用了其它的步骤, 那么未预料到的 非数字输入会不断 "阻塞" scanf()

scanf() 永远都不能越过错误的非数字字符而处理后边的合法数字字符。

如果用户在数字格式的 scanf 如 %d 或 %f  中输入字符 `x', 那么提示后并用同样的 scanf() 调用重试的代码会立即遇到 同一个 'x'。 